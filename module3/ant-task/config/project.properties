build.path=build
dist.path=dist/${DSTAMP}_${TSTAMP}
lib.src=lib
lib.dist=${dist.path}/lib
project.src=src
project.dest=${build.path}/project
project.dist=${dist.path}
test.src=test
test.dest=${build.path}/test
