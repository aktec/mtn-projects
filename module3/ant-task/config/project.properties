tstamp=2015/08/14 12\:43

build.classes=build/classes
build.jar=build/jar
artifact=dist/${tstamp}

lib.src=lib
lib.artifact=${artifact}/lib

project.src=src
project.dest=${build.classes}/project
project.jar=${build.jar}

test.src=test
test.dest=${build.classes}/test
